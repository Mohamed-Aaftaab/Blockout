{
  "name": "blockout",
  "version": "1.0.0",
  "description": "Blockout — autonomous AI trading agent that blacks out MEV bots on BNB Smart Chain",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node -r dotenv/config dist/index.js",
    "dev": "ts-node -r dotenv/config src/index.ts",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "typecheck": "tsc --noEmit"
  },
  "_comments": {
    "hackathon-sdks": "@trustwallet/agent-sdk and @bnb-chain/bnbagent-sdk are hackathon SDKs. The agent uses ethers.js v6 for equivalent self-custody signing (TradingEngine + ExecutionService). When these packages are published, replace the ethers wallet logic with TWAK AgentKit calls.",
    "twak-note": "TWAK_ACCESS_ID and TWAK_HMAC_SECRET are pre-loaded in config for TWAK integration readiness. Currently the agent uses a persistent ethers.Wallet stored in data/wallet.key for equivalent self-custody functionality."
  },
  "dependencies": {
    "axios": "^1.6.7",
    "dotenv": "16.6.1",
    "ethers": "^6.9.2",
    "uuid": "^9.0.1",
    "winston": "^3.11.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.11.5",
    "@types/uuid": "^9.0.7",
    "fast-check": "^3.15.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
